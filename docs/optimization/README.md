# 优化变更工件

每个优化变更包都必须使用同一标识创建三份工件：

1. `issues/<id>.md`：问题、证据、范围与验收条件。
2. `specprs/<id>.md`：方案、文件级修改计划、风险与回滚边界。
3. `implprs/<id>.md`：实际改动、验证命令、结果与残留风险。

实现前必须完成 Issue 与 SpecPR；实现后才可写 ImplPR。一次变更包只解决一个有证据的优化目标，不混入无关重构。
