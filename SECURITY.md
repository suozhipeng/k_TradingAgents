# 安全政策

## 1. 报告安全漏洞

如发现安全漏洞，请通过以下方式报告：

- **GitHub Issues**：创建 Issue 并标记为 security advisory
- **邮件**：联系项目维护者

请不要在公开 issue 或讨论区披露安全漏洞细节。

## 2. 当前安全措施

### 2.1 数据本地化

- 所有数据存储在本机（DuckDB、缓存、配置文件）
- 不向任何第三方服务器上传用户数据
- API Key 仅存储在本地 `.env` 文件

### 2.2 交易安全

- 默认安全模式：所有执行操作需要人工确认
- Kill Switch：一键阻断所有后续交易
- 风控门：每笔交易前的自动检查
- Advisory-only：AI 输出默认不触发真实交易

### 2.3 路径遍历防护

- ticker 输入经过路径遍历验证
- 防止恶意 ticker 逃逸预期目录

## 3. 已知安全问题

| 问题 | 状态 | 缓解措施 |
|---|---|---|
| LangChain Core 漏洞 (LangGrinch) | 已修复 (v0.2.1) | 升级到最新版本 |
| Chainlit CVE-2026-22218 | 已修复 (v0.2.1) | 已移除 chainlit 依赖 |
| Windows UTF-8 编码问题 | 已修复 (v0.2.4) | 升级到最新版本 |

## 4. 依赖安全

- 使用 `uv.lock` 锁定精确版本
- 定期检查依赖更新
- 建议使用 Dependabot 或类似工具监控依赖安全

## 5. 安全最佳实践

- 不要将 `.env` 文件提交到版本控制
- 定期更新依赖包
- 不在生产环境使用 debug 模式
- 定期备份 DuckDB 数据文件

## 6. 安全审计

当前项目暂无独立的安全审计报告。
进入企业交付阶段时，建议进行正式的安全评估和渗透测试。
