# 开发快照 — 2026-07-24（冻结）

> 状态：**FROZEN**。除创建本快照、提交和推送外，不再继续修改代码、运行服务、写入数据库或执行 LR Gate。

## 1. 冻结定位

| 项目 | 实测值 |
|---|---|
| 时间 | 2026-07-24 17:37 HKT |
| 仓库 | `k_TradingAgents` |
| 集成工作树 | `.worktrees/xg-integration` |
| 当前分支 | `fix/xg-dev-data-loop-v033` |
| 冻结前 HEAD | `1d4bb188451bf45d174faa3c33656da7784dfc4c` |
| 主分支远端 | `origin/xg_dev` = `02effa2f38466c021ae9ef308888e67203499a41` |
| Merge-base | `ae9c22f759351820cd152952b59099d7b8b14121` |
| 分叉计数 | `origin/xg_dev...HEAD = 3 5` |

说明：当前集成分支比 `origin/xg_dev` 多 5 个提交，而 `origin/xg_dev` 有 3 个当前分支未包含的提交。恢复后必须先审查分叉提交，不可直接合并。

## 2. 分支独有提交（冻结前）

```text
1d4bb18 feat: 加载四大指数数据(akshare) — 上证/深证/沪深300/创业板
54e9bb9 feat: V1.7 POST /api/v1/analysis/stocks — 个股分析API(单标+批量)
5ae283f chore: 标记auth测试为skipif(local-release无需auth)
99113ca refactor: 底层清理 — 旧Router从604行砍到100行，移除路由策略/缓存/质量/限流层
3713ca7 fix: V1.7 after_request 自动包裹 JSON 到 Envelope — 所有 API 兼容前端 APIClient
```

注意：`1d4bb18` 实际修改的是 local-release evidence 与 verifier 的备份 DB 排除规则；四大指数是临时写入本机 DuckDB，不在该提交或任何 Git 文件中。提交标题不代表存在可复现的指数导入实现。

## 3. 本机运行时数据（不提交）

Canonical DB：`~/.tradingagents/astock/astock.duckdb`

| 指标 | 冻结时实测值 |
|---|---:|
| DB 大小 | 18,100,224 bytes |
| `kline_bars` 总行数 | 66,794 |
| `000001.SH` 上证指数 | 8,688 行；2026-07-23；close 3876.777 |
| `399001.SZ` 深证成指 | 8,596 行；2026-07-23；close 14123.307 |
| `000300.SH` 沪深300 | 5,955 行；2026-07-23；close 4728.002 |
| `399006.SZ` 创业板指 | 3,920 行；2026-07-23；close 3575.52 |
| `600519.SH` 贵州茅台 | 618 行；2026-07-23；close 1292.01 |

服务已经停止，`127.0.0.1:5860` 无监听进程，DB 连接锁已释放。

## 4. 已证实的问题

### P0 — 市场追踪板块显示四大指数为 0

- `GET /api/v1/market/kline?symbol=<四大指数>&interval=1d` 可以读出 DB 数据。
- `GET /api/v1/market/overview` 返回 `source=mock`、`is_mock=true`，页面市场摘要显示 0。
- 直接原因：市场总览仍通过旧 `router.get_market_summary()` 路径，而非 V1.7 Store/Repository。
- 涉及文件：`tradingagents/astock/api/routes_market_data.py`、`tradingagents/astock/data_sources/router.py`、`tradingagents/astock/data_sources/router_v17.py`。
- 恢复后顺序：先写 DB 驱动的 overview 回归测试 → 追踪 mock fallback → 将读取迁移至 V1.7 Store/Repository → API + 浏览器验证。

## 5. 验收状态（不得夸大）

| Gate | 状态 | 冻结说明 |
|---|---|---|
| 数据循环 verifier | 待复核 | 最近运行 `262 passed, 3 failed`，不是全绿基线 |
| Browser E2E | 冻结 | 未在本轮运行 |
| 真实 LLM 验收 | 冻结 | 未保留可复现且脱敏的证据 |
| Provider 真实验收 | 部分 | 临时导入指数成功，但非受控产品链路 |
| 重启持久化 | 待复核 | 本轮未在干净环境复验 |
| 供应链安全 | 待复核 | `pip-audit` 未形成证据 |
| Codex Acceptance | BLOCKED/待复核 | 历史运行受沙箱端口限制，不能视作端到端 PASS |

`.hermes-workflow/evidence/local-release/overview.md` 中的 `LOCAL_RELEASE_READY` 和 G12 ✅ 是历史性声明；因实际市场页面仍为 mock，冻结时应视为**需要复核**，不是当前发布结论。

## 6. 恢复步骤

1. `git status --short --branch`，确认冻结提交之后没有意外工作区改动。
2. `git fetch origin --prune`，使用 `git log --left-right --graph origin/xg_dev...fix/xg-dev-data-loop-v033` 审查分叉。
3. 保持服务停止时执行 DB 只读检查；不要将 `~/.tradingagents`、`backups/` 或任何 DuckDB 文件加入 Git。
4. 为四大指数建立可复现的受控导入流程；临时直写 DB 是一次性现场数据，不能当正式实现。
5. 先修复 `/api/v1/market/overview` 的 mock fallback，再恢复其他 LR Gate。

## 7. 冻结边界

- 本次冻结仅推送集成分支快照，不合并到 `xg_dev`。
- 主工作树 `xg_dev` 存在用户已有未提交改动：`scripts/xg_recovery_tick.py`、`docs/RELEASE_CHECKLIST.md`、`scripts/verify_final_release_readiness.sh`；本次未读取、未修改、未提交。
- 不包含密码、Token、API Key、`.env`、本地 DB 或备份二进制文件。
